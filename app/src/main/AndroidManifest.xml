<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:name=".MerlinApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Merlin">

        <!-- WorkManager-Default-Init deaktivieren: MerlinApp liefert die
             Konfiguration (inkl. HiltWorkerFactory für MutationDrainWorker)
             selbst über Configuration.Provider; siehe MerlinApp.kt. -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.Merlin.Starting">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Vom AlarmManager getriggerter Empfänger für Reminder-Notifications
             (Äquivalent zum UNCalendarNotificationTrigger-Callback auf iOS);
             siehe ReminderService.kt/ReminderBroadcastReceiver.kt. -->
        <receiver
            android:name=".data.ReminderBroadcastReceiver"
            android:exported="false" />

        <!-- Share-Target, Äquivalent zu MerlinShare-Extension (iOS). Läuft als
             normale Activity im Haupt-App-Prozess statt als eigene Extension
             (siehe ShareActivity.kt/ShareViewModel.kt – todo.md Abschnitt 10). -->
        <activity
            android:name=".ui.share.ShareActivity"
            android:exported="true"
            android:theme="@style/Theme.Merlin"
            android:excludeFromRecents="true"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>

    </application>

</manifest>
